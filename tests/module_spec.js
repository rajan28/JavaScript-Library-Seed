describe("Testing firstFunc", function() {
	it("should return its input", function() {
		var x = MyFirstLib.firstFunc(5);
		expect(x).toBe(5);
	});
});

// Delete this spec (and util_spec) when you feel comfortable with its structure.